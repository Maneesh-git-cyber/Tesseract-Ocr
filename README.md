# Tesseract-Ocr
This project shows how to create digital soft copy using tesseract lib in python
# Prerequisites :
Use google collab to run the file. To install dependencies use the below commands:

!apt-get update
!apt-get install tesseract-ocr
!pip install pytesseract
